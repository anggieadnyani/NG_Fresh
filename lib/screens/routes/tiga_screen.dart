import 'package:flutter/material.dart';

class TigaScreen extends StatelessWidget {
const TigaScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(
      child: const Icon(Icons.access_alarms_sharp),
    );
  }
}